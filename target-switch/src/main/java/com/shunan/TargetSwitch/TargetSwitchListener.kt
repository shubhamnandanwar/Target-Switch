package com.shunan.TargetSwitch

interface TargetSwitchListener {
    fun onSwitch(is_night: Boolean)
}
/*

fun x() {

    val height = 48

    val clouds_bitmap_left = (height / 2 - value * (height / 2)).toInt()
    cloudsBitmap.setBounds(clouds_bitmap_left, 0, clouds_bitmap_left + height, height / 2)
    cloudsBitmap.alpha = cloudBitmapAlpha()
    cloudsBitmap.draw(canvas)
}*/
